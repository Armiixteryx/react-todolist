import React from 'react';

const Task = () => {
    return (
        <div>
            <input type="checkbox" />Task text
            <button id="change-task-text" />
            <button id="delete-task" />
        </div>
    );
}

export default Task;
